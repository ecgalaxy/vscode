---
- name: Converge
  hosts: all
  tasks:
    - name: "Include vscode"
      include_role:
        name: "vscode"
  environment:
    http_proxy: "{{ lookup('env', 'http_proxy') }}"
    https_proxy: "{{ lookup('env', 'https_proxy') }}"
    no_proxy: "{{ lookup('env', 'no_proxy') }}"
